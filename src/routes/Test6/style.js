import { css } from 'react-emotion'

export const cssVideoContainer = css({
  paddingTop: '56.25%',
  height: 0,
  position: 'relative',
})

export const cssVideo = css({
  width: '100%',
  height: '100%',
  position: 'absolute',
  top: 0,
  left: 0,
})
