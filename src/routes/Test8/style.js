import { css } from "react-emotion";

export const cssForm = css({
  marginTop: 24,
  'input, textarea, button': {
    display: 'block',
    marginBottom: 8,
  }
});


export const cssModal = css({
  marginTop: 24,
  button: {
    marginTop: 8,
  }
});
